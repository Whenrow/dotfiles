#!/bin/sh
# if no argument, take current git branch
if [ -z "$1" ]; then
    DB_TO_BACKUP=$(git rev-parse --abbrev-ref HEAD)
else
    DB_TO_BACKUP=$1
fi
TMPL=$DB_TO_BACKUP-bk
dropdb --if-exists $DB_TO_BACKUP
createdb -T $TMPL $DB_TO_BACKUP
